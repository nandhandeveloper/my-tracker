import React from 'react';
import { Typography } from '@material-ui/core';

const AddStoryForm: React.FC<{}> = () => {
    return (
        <>
            <Typography variant="h6">Hello from Stories</Typography>
        </>
    );
};

export default AddStoryForm;
